# ケロッと！はらぺこエコガエル - Python依存パッケージ一括インストール用
#
# 【構成メモ】ホストPC（RealSense・サーボ用Arduino接続）と仮想マシン
# （Flask・カウント用Arduino接続、2年生B担当）でそれぞれ必要なパッケージが異なります。
# 全部まとめてインストールしても害はありませんが、時間が無い場合は該当セクションだけでOKです。
#
# 使い方：
#   pip install -r requirements.txt
#
# torch はサイズが大きいので、時間が無い場合はCPU版で十分です
# （画像分類モデルの推論だけなのでGPUは必須ではありません）。

# ============================================================
# ホストPC用（ai_core/State machine.py, server/serial_control.py）
# ============================================================
pyrealsense2
opencv-python
transformers
torch
pillow
numpy
mediapipe
pyserial
requests

# ============================================================
# 仮想マシン用（server/app.py, server/sensor_bridge.py）
# ============================================================
flask
flask-cors
pyserial
requests
